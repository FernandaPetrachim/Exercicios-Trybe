let speed = 90;
const speedCar = (speed) => {
  if (speed >= `120`) {
    return `Você ultrapassou o limite de velocidade`;
  } else {
    return `Você está na velocidade permitida`;
  }
};
console.log(speed === speed.speedCar) ? `Você ultrapassou o limite de velocidade`
 : ((spedd === speed) ? `Você está na velocidade permitida`);




